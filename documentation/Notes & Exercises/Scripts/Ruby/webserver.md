## Ruby Webserver

```ruby
ruby -run -e httpd . -p 8000
# Options
-run		# Loads un.rb, which contains replacement components for Unix commands
-e httpd	# run the HTTP server
.			# server content from current directory
-p 8000		# Listen on port 8000
```

